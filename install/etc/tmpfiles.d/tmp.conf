# Overrides /usr/lib/tmpfiles.d/tmp.conf (10d / 30d) - shrinks the window
# /tmp and /var/tmp hold stale files before systemd-tmpfiles-clean.timer
# (daily, built into systemd) sweeps them.
q /tmp 1777 root root 2d
q /var/tmp 1777 root root 2d
