# journald keeps sudo/login/service logs indefinitely by default (bounded
# only by a % of disk, effectively unbounded in practice). Capped to 1 day /
# 100M - same-day crash debugging still works, nothing older sticks around.
[Journal]
SystemMaxUse=100M
MaxRetentionSec=1day
