# pacman-contrib ships paccache.timer set to OnCalendar=weekly. Overridden to
# daily so package-cache pruning runs on the same cadence as trash-cleanup
# and systemd-tmpfiles-clean, instead of leaving old package versions in
# /var/cache/pacman/pkg for up to a week.
[Timer]
OnCalendar=
OnCalendar=daily
