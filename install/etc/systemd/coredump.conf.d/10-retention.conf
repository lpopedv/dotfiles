# A coredump is a full memory snapshot of whatever crashed - same idea as
# the journald cap, applied here. Storage stays external (still useful for
# debugging a crash the same day); MaxUse bounds total size, and
# tmpfiles.d/coredump.conf ages individual files out after 2 days.
[Coredump]
MaxUse=200M
